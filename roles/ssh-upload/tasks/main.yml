---
  
- name: add second ssh key for mobile access
  become: yes
  authorized_key:
    user: "{{ user_name }}"
    state: present
    manage_dir: yes
    key: "{{ lookup('file', '../../../../../../Ansible-Config/id_rsa.pub') }}"
    
    
 
